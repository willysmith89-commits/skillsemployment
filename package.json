{
  "name": "skillsemployment-backend",
  "version": "1.0.0",
  "description": "Stripe webhook + HubSpot deal endpoint for skillsemployment.com.au",
  "private": true,
  "engines": { "node": "20" },
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "stripe": "^17.0.0"
  }
}
